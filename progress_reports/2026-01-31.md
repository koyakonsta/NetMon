There hadn't been progress in the earlier parts of this month as we were both enrolled in the Machine Learning module, and were busy with that.
Now that it's done, we intend to work on the third year project full-time.

We have chosen to do the UI using NativeScript-Vue and created a mockup of the home page and device list.
The devices page has a button to mark a device as safe or unsafe.

We are now planning to start working on the network side, to detect the devices that are connected to the current network.
We aim to do this by adding a background process that will listen in on device network packets,
detect incidents and update the GUI accordingly. This will most likely be done through a VPNService to capture all relevant traffic.