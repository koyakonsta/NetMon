We have submitted the functional specification on Gitlab.

We decided to add the communication between devices on the local network into the functional spec.
However, we left a comment that more could potentially be added if we have the time.